# CI/Test environment variables for act
# These override your local .env when running GitHub Actions locally with act

DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Keep cache and sessions as file-based for testing
CACHE_STORE=file
SESSION_DRIVER=file
QUEUE_CONNECTION=sync
